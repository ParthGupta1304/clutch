rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Commitments collection
    match /users/{userId}/commitments/{commitmentId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
